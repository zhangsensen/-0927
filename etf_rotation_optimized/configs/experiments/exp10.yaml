# Exp10: threshold=0.88, beta=0.8, max=8
# 目标: 降低去重力度 + 启用Meta Factor

correlation_deduplication:
  threshold: 0.88
  strategy: "keep_higher_ic"

meta_factor_weighting:
  enabled: true
  beta: 0.8

minimum_ic:
  global_minimum: 0.012
