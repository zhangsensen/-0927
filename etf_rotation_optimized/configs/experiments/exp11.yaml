# Exp11: threshold=0.90, beta=0.8, max=8
# 目标: 进一步降低去重力度 + 启用Meta Factor

correlation_deduplication:
  threshold: 0.90
  strategy: "keep_higher_ic"

meta_factor_weighting:
  enabled: true
  beta: 0.8

minimum_ic:
  global_minimum: 0.012
